export const metadata = {
  title: "TVK - Gallery",
};

export default function GalleryLayout({ children }) {
  return (
    <html lang="en">
      <body className="">{children}</body>
    </html>
  );
}
